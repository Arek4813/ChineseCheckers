package board.pawn;

import javafx.scene.shape.Circle;

/**
 * Creating an object extending after Circle, which is used as pawn field in game - in other classes are made
 * different pawn colors (where classes extends after Pawn class
 */
public abstract class Pawn extends Circle {
    private static final Integer RADIUS=9;

}
